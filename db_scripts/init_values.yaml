dbUserPasswords:
  dbuserPassword: ""

databases:
  inji_verify:
    enabled: true
    host: "postgres-postgresql.postgres"
    port: 5432
    su:
      user: postgres
      secret:
        name: postgres-postgresql
        key: postgres-password
    dml: 1
    repoUrl: https://github.com/mosip/inji-verify.git
    branch: develop
