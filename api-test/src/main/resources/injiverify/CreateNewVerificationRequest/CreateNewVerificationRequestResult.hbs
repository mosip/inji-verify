{
    "response": {
        "authorizationDetails": {
            "responseType": "{{responseType}}",
            "responseUri": "{{responseUri}}",
            "clientId": "{{clientId}}",
            "presentationDefinition": {
                "id": "{{id}}"
            }
        }
    }
}